## Validation: issue #22 — PASS
Spec-anchored check: no schematic spec.md for this issue (entered via `task` directly) - fell back to "assertion exists and covers the criterion" for all 3 acceptance criteria (all 3 confirmed by an independent spec-compliance reviewer, second pass, after the benchmark-impact gap was closed).
Mutation sensor (mandatory): EMPTY_RETURN=KILLED, IDENTITY_RETURN=KILLED, NEGATE_CONDITIONAL=KILLED, DROP_SIDE_EFFECT=N/A: pack()/count_tokens() are pure functions, no side effect in the changed code paths
Mutation sensor (extras): 4 injected, 4 killed, 0 survived
Mandatory detail: EMPTY_RETURN (count_tokens->0) killed by test_count_tokens_is_real_bpe_not_char_quarter + test_pack_honors_injected_cost. IDENTITY_RETURN (count_tokens->estimate_tokens, skipping real tokenization) killed by 9 tests. NEGATE_CONDITIONAL (pack's `>` to `>=`) killed by the existing pinned test_pack_keeps_segments_within_budget.
Extras detail: wiring-drop in glyph/retrieval/graph.py killed by test_graph_arm_packs_with_real_tokens (added in a fix round). Wiring-drop in glyph/retrieval/hybrid.py killed by test_hybrid_arm_packs_with_real_tokens. Wiring-drop in glyph/retrieval/community.py killed by test_community_arm_packs_with_real_tokens. Wiring-drop in glyph/retrieval/path.py's "len(top2)<2" branch initially SURVIVED (0 failures) - closed via a second fix round adding test_path_arm_fewer_than_two_anchors_packs_with_real_tokens and test_path_arm_no_path_between_anchors_packs_with_real_tokens for path.py's other 2 branches; re-verified killed.
Reviewer rounds: code-quality (codex/gpt-5.6-sol, xhigh) - round 1 BLOCK (3 findings: special-token crash in count_tokens, tiktoken undeclared in pyproject.toml, unbounded cold-start network fetch) -> fixed (disallowed_special=(), tiktoken>=0.7 added to core deps, documented limitation for the network-fetch caveat) -> not re-dispatched formally but fixes verified directly (gate green, diff inspected). Spec-compliance (Sonnet, subagent) - BLOCK (AC3 benchmark-impact not documented) -> fixed (ADR amendment gained a "Benchmark impact" subsection with measured, honestly-reported real-vs-char token deltas in both directions) -> verified directly (diff read, gate green).
Gate: ruff + ruff format + mypy clean; pytest 374 passed, 19 deselected; coverage 96.99% (>= 90% required).
Report: .specs/features/issue-22-real-tokenizer/validation.md

## Validation: issue #16 — PASS
Spec-anchored check: no spec.md for this issue (entered via `task` directly) — fallback: assertion exists and covers each criterion. Legendary Plan stage chose 3 target functions (one per limb: document/code/retrieval) with a criteria-mapped test-plan table; all bullets mapped to tests, no exemptions needed (test-only issue).
Mutation sensor (mandatory): EMPTY_RETURN=KILLED, IDENTITY_RETURN=KILLED, NEGATE_CONDITIONAL=KILLED, DROP_SIDE_EFFECT=N/A: all three targets (_strip_frontmatter, _parse, _extract_named_entities) are pure functions with no side effects to drop
Mutation sensor (extras): 5 injected (Legendary bar: 4+ targeting the risk area), 5 killed after 2 fix rounds: multi_anchor.py EMPTY_RETURN (risk-area target) KILLED after fix, multi_anchor.py NEGATE_CONDITIONAL (dedup-guard drop, risk-area target) KILLED, extractor.py ignore-input mutation KILLED after fix, markdown.py closing-delimiter regression-to-#33-bug KILLED via deterministic pin, markdown.py closing-delimiter over-tightening KILLED after fix
Report: .specs/features/issue-16-hypothesis-extraction-retrieval/validation.md

## Mandatory battery detail, per target
- `_strip_frontmatter` (markdown.py): EMPTY_RETURN (always return "") = KILLED (fails identity + frontmatter-block-to-body properties). IDENTITY_RETURN (return text unchanged) = KILLED (fails frontmatter-block-to-body). NEGATE_CONDITIONAL / injection analog (replace matched block with a non-empty marker instead of removing) = KILLED (fails suffix + frontmatter-block-to-body properties). DROP_SIDE_EFFECT = N/A, pure function.
- `_parse` (extractor.py, tree-sitter wrapper): EMPTY_RETURN analog (stub with `root_node=None`) = KILLED. IDENTITY_RETURN analog (ignore real input, always parse a fixed constant) = SURVIVED on first pass (the only test checked `root_node is not None`, never that the tree reflects the actual input) — FIXED by adding `tree.root_node.end_byte == len(source)`, a real tree-sitter invariant (verified against empty/malformed/non-UTF8 garbage bytes on the real implementation); re-verified KILLED. DROP_SIDE_EFFECT = N/A, pure function.
- `_extract_named_entities` (multi_anchor.py, the `retrieval` risk-area target): EMPTY_RETURN (always return `[]`) = SURVIVED on first pass — classic vacuous-shape trap (empty list is trivially "distinct" and trivially "all substrings"), on the exact risk-area function this Legendary pass was scoped to scrutinize. FIXED by adding a concrete presence test pinning `_extract_named_entities("see dec-121 and SomeCamelCaseWord for context") == ["dec-121", "SomeCamelCaseWord"]`; re-verified KILLED. NEGATE_CONDITIONAL (drop the `if e not in seen` dedup guard) = KILLED (fails the distinctness property). DROP_SIDE_EFFECT = N/A, pure function.

## Extras detail (Legendary bar: 4+ mutations targeting the risk area itself)
1. multi_anchor.py EMPTY_RETURN — risk-area target, KILLED after fix (see above).
2. multi_anchor.py NEGATE_CONDITIONAL (dedup-guard drop) — risk-area target, KILLED.
3. extractor.py ignore-input mutation — adjacent extraction target, KILLED after fix (see above).
4. markdown.py: revert the #33 fix (`[^\S\n]*` back to `\s*` on the closing delimiter, i.e. reintroduce the real #33 bug) — the hypothesis-sampled property test alone was NOT reliably catching this every run (observed flakiness across repeated local runs, `.hypothesis` example cache present); FIXED by adding a deterministic (non-hypothesis) test pinning the exact `"---\n\n---\n\n" -> "\n"` counterexample; re-verified KILLED 3/3 consecutive runs.
5. markdown.py: over-tighten the #33 fix (drop `[^\S\n]*` entirely, losing the legacy "trailing horizontal whitespace on the closing `---` line is still stripped" behavior) — SURVIVED on first pass (no test pinned the legacy behavior at all); FIXED by adding a concrete test (`"---\nkey: v\n---  \t\nbody" -> "body"`); re-verified KILLED.

## Findings surfaced during this Legendary pass (both owner-decided, both resolved)
- Issue #32: the originally-planned idempotence property for _strip_frontmatter was WRONG (not a code bug) — the function is contractually single-leading-block; remaining `---` lines after a strip are legitimate Markdown thematic breaks. Owner decision: drop idempotence, replace with identity/suffix/frontmatter-block->body properties. Applied.
- Issue #33: implementing the owner's frontmatter-block->body property (#32) surfaced a REAL bug — the closing delimiter's `\s*` (greedy, matches \n) swallowed leading blank body lines. Owner decision: fix the regex (`\s*` -> `[^\S\n]*` on the closing delimiter only). Applied as a one-line change to `glyph/extract/document/markdown.py`, verified against the exact counterexample deterministically, plus a regression test that doesn't depend on hypothesis's random sampling to rediscover it.

All scratch mutations were applied directly to the real source files (markdown.py, extractor.py, multi_anchor.py) and reverted immediately after each check; `git diff`/`diff` against pre-mutation copies confirmed byte-identical restoration after every mutation. Final `git status --porcelain` scope: pyproject.toml (+hypothesis dep), glyph/extract/document/markdown.py (#33 one-line fix), 3 new test files. Gate: 347 passed, 19 deselected, 96.91% coverage.

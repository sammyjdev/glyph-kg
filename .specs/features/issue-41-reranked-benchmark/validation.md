## Validation: issue #41 — PASS
Spec-anchored check: no schematic spec.md for this issue (entered via `task` directly, owner decision already recorded in the issue's 2026-07-20 comment) - fell back to "assertion exists and covers the criterion"; both independent reviewers confirmed all 3 owner-set criteria against the actual measured numbers: (1) reranked_vector arm benchmarked with HF_HUB_DISABLE_XET=1, (2) METRICS.md/README.md/eval/benchmark-baseline.json re-baselined with real numbers, (3) context_precision (0.720) reported against the 0.55+ target and against this run's own vector baseline (0.400); BM25 explicitly not implemented (out of scope, confirmed via `git diff --name-only` showing no code files).
Mutation sensor (mandatory): EMPTY_RETURN=N/A: no code changed this pass (only reran the existing unmodified scripts/run_benchmark.py and edited one README prose bullet), IDENTITY_RETURN=N/A: no code changed this pass, NEGATE_CONDITIONAL=N/A: no code changed this pass, DROP_SIDE_EFFECT=N/A: no code changed this pass
Mutation sensor (extras): 0 injected, 0 killed, 0 survived - N/A, no code diff to mutate; risk_area_hit was on eval-baselines by data content (owner-sanctioned regeneration for this issue), not by code logic.
Reviewer rounds: spec-compliance (Sonnet, subagent) - APPROVE (verified eval/benchmark-baseline.json and METRICS.md match exactly, reranked_vector context_precision 0.720 [0.560,0.880] clears the 0.55+ target with the CI lower bound alone, README bullet matches the JSON, no .py file touched, docs/article.md and docs/portfolio.md untouched, gate green). Code-quality (codex/gpt-5.6-sol, high effort) - APPROVE (JSON validity, CI bounds bracket the mean for every metric, token/cost arithmetic consistent with glyph/eval/cost.py's fixed Haiku-4.5-rate convention, no secrets/absolute-path leakage, README/METRICS consistent).
Gate: ruff + mypy clean; pytest 362 passed, 19 deselected (unchanged from baseline - no code diff); coverage 96.83% (>= 90% required).
Report: .specs/features/issue-41-reranked-benchmark/validation.md

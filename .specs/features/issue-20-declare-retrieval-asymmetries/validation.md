## Validation: issue #20 — [PASS]
Spec-anchored check: no spec.md for this issue (entered `task` directly) - fell back to assertion-exists-and-covers-the-criterion, applied as mechanical substring checks since this is a docs-only deliverable. AC1 (embedding-surface asymmetry + design rationale) confirmed present in docs/article.md's "Declared limitations" section: contains "embeds only node labels", "neighbour expansion", and "representation-only". AC2 (coverage-per-budget difference + recall@budget effect) confirmed: contains "Coverage per budget", "Node.attrs", "more segments", and "recall@budget". Both bullets verified (sed range check) to sit inside the "## Declared limitations" section, before "## Reproduce". 2/2 ACs matched.
Mutation sensor (mandatory): EMPTY_RETURN=N/A: non-code deliverable (docs-only prose change, no function/return value), IDENTITY_RETURN=N/A: non-code deliverable, NEGATE_CONDITIONAL=N/A: non-code deliverable, DROP_SIDE_EFFECT=N/A: non-code deliverable
Mutation sensor (extras): N/A: non-code deliverable (Legendary tier forced mechanically by the `retrieval` risk-area NAME match in the issue title, not by structural/code risk - the actual change is a 2-bullet markdown append with no logic to mutate)
Report: .specs/features/issue-20-declare-retrieval-asymmetries/validation.md

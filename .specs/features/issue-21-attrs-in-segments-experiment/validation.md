## Validation: issue #21 — [PASS]
Spec-anchored check: no spec.md for this issue (entered `task` directly) - fell back to assertion-exists-and-covers-the-criterion, with a ratified interpretation call recorded (recall@budget is deterministic/judge-free per ADR-G8, so AC1's "benchmark run" was satisfied by a retrieval-only comparison, not a live generation+judge run - see docs/decisions/phase3-attrs-in-segments.md). AC1 (benchmark run comparing segments with vs without attrs) covered by scripts/attrs_recall_experiment.py, actually executed against the real out/monster-manual.json graph. AC2 (recall@budget delta reported for both arms) covered by the real measured numbers (baseline 0.3485, attrs 0.3265, delta -0.0220) recorded in docs/decisions/phase3-attrs-in-segments.md, and by test_rate_on_real_query_set-equivalent exact-value tests on the underlying recall_at_budget function (tests/eval/test_recall.py) plus the segment-builder tests (tests/retrieval/test_graph_attrs.py) that prove the with/without-attrs mechanism the script relies on. AC3 (keep/revert decision recorded, declaration doc updated if kept) covered by docs/decisions/phase3-attrs-in-segments.md (REVERT decision, explicit threshold rationale) and docs/decisions/phase3-quality-backlog.md item 4 updated to record the resolution. 3/3 ACs matched.
Mutation sensor (mandatory): EMPTY_RETURN=KILLED, IDENTITY_RETURN=KILLED, NEGATE_CONDITIONAL=KILLED, DROP_SIDE_EFFECT=KILLED
Mutation sensor (extras): 4 injected, 4 killed, 0 survived: [E1 include_attrs default flipped False->True in GraphRetriever.__init__ (glyph/retrieval/graph.py) - a risk-area regression (retrieval) since every existing production call site relies on the default - KILLED via tests/retrieval/test_graph_attrs.py::test_default_omits_attrs_unchanged and test_attrs_reduce_segments_under_tight_budget; E2 attrs_str separator/format changed ("; "/"k: v" -> ", "/"k=v") - KILLED via test_include_attrs_appends_attr_values and test_include_attrs_with_no_relations exact-string assertions; E3 ground-truth validation assert removed from recall_at_budget (glyph/eval/recall.py) - KILLED via test_recall_empty_relevant_asserts (raises ZeroDivisionError instead of the expected AssertionError, proving the check is load-bearing); E4 BOUNDARY on the attrs-inclusion guard (node.attrs truthy -> len(node.attrs) > 1) - KILLED via three tests using single-attr fixtures]
Report: .specs/features/issue-21-attrs-in-segments-experiment/validation.md
